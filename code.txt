#include <stdio.h>
int main() {    

    int number1, number2, sum, constant;
    int B;
    
    printf("Enter two integers: version Alex-55");
    scanf("%d %d", &number1, &number2);

    // calculating sum (multiplication khaled)
    sum = number1 * number2;      
    
    printf("%d + %d = %d", number1, number2, sum);
    return 0;
}